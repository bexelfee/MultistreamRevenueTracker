"""Multistream revenue tracker package."""

# Single source of truth for the app version. The UI About panel and PyInstaller
# build scripts both read from this value so a release bump only needs to touch
# this file. Update when shipping a new release.
__version__ = "1.0.0"

